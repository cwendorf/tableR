% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/styler.R
\name{style_plain}
\alias{style_plain}
\title{Format a 'tableR' Object as Plain Text}
\usage{
style_plain(formatted, caption = NULL, space = c(1, 1))
}
\arguments{
\item{formatted}{A \code{"tableR"} object (or \code{"tableR_list"}) created by \code{format_table()}.}

\item{caption}{Optional character string specifying a caption to print above the table.
Defaults to the \code{"caption"} attribute of the object, if any.}

\item{space}{Integer vector of length 1 or 2 specifying the number of blank lines to add
before and after the table output. If length 1, the value is recycled.}
}
\value{
Invisibly returns a character vector of the plain-text table lines.
        Also prints the table to the console or knitr output.
}
\description{
Formats a \code{"tableR"} object (or \code{"tableR_list"}) into a plain-text,
aligned table suitable for printing in the R console or in knitr output.
This function does not use markdown table syntax and does not rely on monospace fonts.
It produces simple, space-padded, readable output.
}
